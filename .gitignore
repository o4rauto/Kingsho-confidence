# Dependências (o projeto não usa, mas previne commits acidentais)
node_modules/

# Logs
*.log
npm-debug.log*

# Sistema operacional
.DS_Store
Thumbs.db

# Editores
.vscode/
.idea/
*.swp
*~

# Saves locais / temporários
*.local

# Artefatos de verificação QA (gerados por verify.js / verify-webgl.js;
# o CI os publica como artifacts — não versionar)
verify_screenshot.png
tools/webgl_spike_screenshot.png

# Lockfile (projeto sem build; CI usa npm install)
package-lock.json
